RiseUp Playbuilder patch

Changes:
- Uses values extracted from rise_playable_1783594349182.html as editor defaults.
- Default stage count is 1, with the 20-obstacle mini-level from the playable loaded in Level Editor.
- Default background gradients, seam overlays, end-card images, player start and gameplay sliders match the playable export.
- Keeps the val/readValue compatibility fix for the preview crash.

Apply:
Unzip this archive over the RiseUp_Playbuilder_Test-09.07.2026 folder and replace files. Then open index.html and hard-refresh the browser if needed.
